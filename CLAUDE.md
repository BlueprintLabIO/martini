use psql and SECRET_DATABASE_URL if necessary to view the db.
do not run ops on the db without asking for permission.
use src/lib/db/schema.ts for basic structure check

run pnpm commands to guarantee latest versions

replace @lucide with lucide-svelte

do not code. do not be lazy, read more.

do not be lazy. ensure your implementation is careful and complete

always plan, explain and get my confirmation.

first reading current code base extensively to not break any existing features or patterns.

always ask me clarifying questions!!!

always find the root cause and read ALL related existing code

always purge old files after refactor or migration.

what is the industry gold standard?

no need to create summary documents all the time.

read ai-sdk-v6-usage-guide.md if the change is anything to do with ai sdk

### UI Component Libraries
- **shadcn-svelte**: Pre-built accessible components (tree-view, collapsible, etc.)
- **CodeMirror 6**: Code editor with JavaScript syntax highlighting
- **bits-ui**: Low-level UI primitives (used by shadcn)
- **lucide-svelte**: Icon library



